export const VAULT_LOSS_STATEMENT =
  "If you lose both your passphrase and your recovery code, these documents are permanently unreadable. No one, including the operator, can recover them. There is no reset.";

export const VAULT_PASSPHRASE_HINT =
  "This is not your sign-in password. A password reset restores the account, not the vault.";

export const VAULT_STAY_UNLOCKED_HINT =
  "Stay unlocked on this device. The key stays in this browser; it is a convenience setting, not extra protection.";

export const VAULT_HISTORY_PURGE =
  "Moving across the vault boundary restarts version history. The previous versions of this essay will be deleted.";

export const VAULT_SERVER_FEATURE_REASON =
  "Unavailable in the vault: this would send the essay to the server.";

export const VAULT_IMAGE_WARNING =
  "Images in vault essays are stored unencrypted.";

export const VAULT_LOCKED_BODY = "Unlock the vault to read this essay.";

export const VAULT_WHAT_WE_SAY =
  "Vault essays are encrypted in your browser before they reach the server. The database stores ciphertext. The operator can see that a document exists and how large it is, but cannot read it. The vault is optional and available on every account.";

export const VAULT_PASSWORD_RESET_NOTE =
  "This resets your sign-in password. A vault passphrase is separate and is not changed here.";

export const VAULT_GITHUB_INCLUDE =
  "Include unlocked vault essays in this push. Only private repositories; a public repo is refused.";
