Some **bold text** and *italic text* and `inline code` and ~~struck through~~.

A [plain link](https://example.com) and a [link with **bold** inside](https://example.org/path?a=1&b=2).

Nested marks: **bold with *italic* inside** and *italic with `code` inside*.

A sentence ending in **bold.**
